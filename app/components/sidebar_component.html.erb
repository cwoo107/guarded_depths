<nav
  class="bg-gray-700 md:left-0 md:block md:fixed md:top-0 md:bottom-0 md:overflow-y-auto md:flex-row md:flex-nowrap md:overflow-hidden shadow-xl bg-white flex flex-wrap items-center justify-between relative md:w-64 z-10 py-4 px-6"
>
  <div
    class="md:flex-col md:items-stretch md:min-h-full md:flex-nowrap px-0 flex flex-wrap items-center justify-between w-full mx-auto"
  >
    <div
      class="md:block text-left md:pb-2 text-yellow-400 mr-0 inline-block whitespace-nowrap text-sm uppercase font-bold p-4 px-0"
    >
      Ticket Delivery
    </div>
    <ul class="md:hidden items-center flex flex-wrap list-none">

      <li class="items-center mx-2">
        <%= link_to root_path do %>
          <div
            class="text-xs uppercase py-3 font-bold block
              <%= controller_name == 'codes' ? ('text-yellow-400 hover:text-yellow-500') : ('text-gray-300 hover:text-gray-500') %>"
          >
            Member Codes
          </div>
        <% end %>
      </li>
      <li class="items-center mx-2">
        <%= link_to tickets_path do %>
          <div
            class="text-xs uppercase py-3 font-bold block
              <%= controller_name == 'tickets' ? ('text-yellow-400 hover:text-yellow-500') : ('text-gray-300 hover:text-gray-100') %>"
          >
            Tickets
          </div>
        <% end %>
      </li>
      <li class="items-center ml-16">
        <%= link_to( destroy_user_session_path, method: :delete) do %>
          <div
            class="text-gray-300 hover:text-gray-100 text-xs uppercase py-3 font-bold block"
          >
            Logout
          </div>
        <% end %>
      </li>
    </ul>
    <div
      class="md:flex md:flex-col md:items-stretch md:opacity-100 md:relative md:mt-4 md:shadow-none shadow absolute top-0 left-0 right-0 z-40 overflow-y-auto overflow-x-hidden h-auto items-center flex-1 rounded hidden"
      id="example-collapse-sidebar"
    >
      <hr class="my-4 md:min-w-full" />
      <ul class="md:flex-col md:min-w-full flex flex-col list-none">
        <li class="items-center">
          <%= link_to root_path do %>
            <div
              class="text-xs uppercase py-3 font-bold block
              <%= controller_name == 'codes' ? ('text-yellow-400 hover:text-yellow-500') : ('text-gray-300 hover:text-gray-500') %>"
            >
              Member Codes
            </div>
          <% end %>
        </li>

        <li class="items-center">
          <%= link_to tickets_path do %>
            <div
              class="text-xs uppercase py-3 font-bold block
              <%= controller_name == 'tickets' ? ('text-yellow-400 hover:text-yellow-500') : ('text-gray-300 hover:text-gray-100') %>"
            >
              Tickets
            </div>
          <% end %>
        </li>
      </ul>
      <hr class="my-4 md:min-w-full" />
      <ul
        class="md:flex-col md:min-w-full flex flex-col list-none md:mb-4"
      >
        <li class="items-center">
          <%= link_to( destroy_user_session_path, method: :delete) do %>
            <div
              class="text-gray-300 hover:text-gray-100 text-xs uppercase py-3 font-bold block"
            >
              Logout
            </div>
          <% end %>
        </li>
      </ul>
      <hr class=" md:min-w-full" />
    </div>
  </div>
</nav>